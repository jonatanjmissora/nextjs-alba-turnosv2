"use server";

export const addTurnoAction = async (
    service: string,
    date: string,
    time: string,
    name: string,
    phone: string,
) => {
    console.log(service, date, time, name, phone);
};
